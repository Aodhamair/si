{% extends 'base.html.twig' %}

{% block body %}

    {% for post in posts %}
       <h1>{{ post.title }}</h1>
        <p>{{ post.content }}</p>
        <p>{{ post.createdAt|date('d-m-Y')}} +++ <a href="{{ url('post_edit',{id:post.id}) }}">EDYTUJ</a> +++ <a href="{{ url('post_delete',{id:post.id}) }}">USUŃ</a> </p>
    {% endfor %}

{% endblock %}